/** Shared sizing/colors for the generated `opengraph-image.tsx` routes —
 *  hex values lifted straight from `globals.css`'s design tokens, not new. */

export const OG_SIZE = { width: 1200, height: 630 };

export const OG_COLORS = {
  primary: "#e4002b",
  primaryHover: "#c40025",
  primarySubtle: "#fdecee",
  primaryOnSubtle: "#c40025",
  ink: "#0b0f1a",
  bg: "#ffffff",
  bgMuted: "#f9fafb",
  textSecondary: "#4B5563",
};
